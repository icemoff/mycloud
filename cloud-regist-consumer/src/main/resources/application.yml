server:
  port: 8090

spring:
  application:
     name: consumer-test

eureka:
  client:
    serviceUrl:
      defaultZone: http://myusername:pass123@localhost:8761/eureka/
  instance:
    prefer-ip-address: true